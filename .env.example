# URL do webhook do CRM Clint. O proxy /api/lead encaminha o lead para cá
# server-side, então a URL real nunca é exposta no navegador.
# Se não definida, usa o endpoint padrão embutido em app/api/lead/route.js.
CLINT_WEBHOOK_URL=https://functions-api.clint.digital/endpoints/integration/webhook/bf9aa1eb-e684-4c0a-803d-2cd8314fc0b2
